import React from "react";
import Sandbox from "../svgsandbox/sandbox1";
const Home = (props) => {
  return (
    <div className="App">
      <header>Welcome to React with D3</header>
      <Sandbox />
    </div>
  );
};

export default Home;
